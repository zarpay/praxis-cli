# TODO

## Roadmap (withdrawn/deferred capabilities live in cli/specs/roadmap/)

- **`praxis calibrate add <target>`** — scaffold a calibration case:
  copy the target as the case's input file, freeze the governing spec
  as `spec.md`, and stub an `expected.json` (empty violation lists,
  `spec_path` filled, adjudicator fields blank) for the human to
  complete. Mechanical copying only — every judgment stays hand-written
  (06: no generated answers in the answer key). Requested during the
  servus adoption, 2026-09-06, after authoring the first cases by hand.
