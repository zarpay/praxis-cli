{
  "version": "2.0",
  "validations": {
    "e280f5fa": {
      "spec_path": "src/features/README.md",
      "document_type": "unknown",
      "cached_at": "2026-08-30T23:07:17.006Z",
      "content_hash": "c604375b",
      "result": {
        "compliant": true,
        "issues": [],
        "reason": "The tasting-menu feature satisfies all structural and cohesion criteria:\n\n1. Structure: Exactly one entry point `index.ts` re-exports the public API (buildMenu run plus types); types live in a dedicated `tasting-menu-types.ts`; logic file `build-menu.ts` is verb-first kebab-case.\n2. Service conventions: `build-menu.ts` exports a single `run` function returning a `Result` (Results over exceptions) with a consumer-grade error message ('stops must be a whole number of 1 or more').\n3. Cohesion: both files are reachable from `index.ts` via its imports (build-menu.ts imported directly; tasting-menu-types.ts imported by both index.ts and build-menu.ts), so no orphaned files. The files serve one capability — building a tasting menu — and the feature name describes the capability, not the implementation.\n\nNo violations found. The feature is judged as a set and conforms to every criterion in the specification. The service conventions reference is external (src/services/README.md) and there is no indication that path exists, but the noted conventions (single run export, Result over exceptions, consumer-grade messages) are all met within the provided files. This does not constitute a deviation requiring correction."
      }
    }
  }
}