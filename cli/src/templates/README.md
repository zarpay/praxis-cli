# templates/ — every file praxis writes

One typed function per emitted document: scaffolded experts and
practices, axiom proposals, the eval-targeting frontmatter, and the
Claude Code plugin's command and skill files. These ship into user
projects — audit them for staleness like user-facing docs, because
they are.

Rule: `.claude/rules/templates.md`.
