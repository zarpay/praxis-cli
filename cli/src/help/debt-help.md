Debt is nonconformance in code that predates its spec: a backlog to
burn down, honestly named. It is never chartable as agent performance —
the code was written before the standard existed. Populations derive
per-axiom from git birthdates, so pre-spec and post-spec violations
never pool.

Docs: https://zarpay.github.io/praxis-cli/commands/debt
