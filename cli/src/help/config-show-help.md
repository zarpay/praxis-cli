When to use: to see the effective config and where it lives.

Behavior:
  Prints the configuration as written, with its file path — no
  defaults filled in, no rewriting. Pure read.

Example:
  $ praxis config show

Docs: https://zarpay.github.io/praxis-cli/reference/config
